apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mysql-fastapi-ingress
spec:
  rules:
    - host: localhost
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mysql-fastapi-service
                port:
                  number: 80
